import 'package:flutter/material.dart';

class OfferedItemsPage extends StatelessWidget {
  const OfferedItemsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Offered Items'),
    );
  }
}
